apiVersion: apps/v1
kind: Deployment

metadata:
  name: config-demo

  labels:
    environment: prod

spec:
  template:
    metadata:
      labels:
        environment: prod

    spec:
      nodeSelector:
        environment: prod
