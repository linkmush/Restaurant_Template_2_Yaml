apiVersion: v1
kind: Service

metadata:
  name: restaurant-service
  labels:
    app: restaurant-template

spec:
  type: ClusterIP

  selector:
    app: restaurant-template

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80